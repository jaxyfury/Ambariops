# -----------------------------------------------------------------------------
# Core Application Configuration
# -----------------------------------------------------------------------------
# REQUIRED: The connection string for your MongoDB database.
MONGODB_URI=mongodb://localhost:27017/amberops

# REQUIRED: A secret key for signing JSON Web Tokens (JWTs).
# Use a long, random string for production.
JWT_SECRET=your-super-secret-jwt-key-that-is-long-and-random

# -----------------------------------------------------------------------------
# Application Ports & URLs
# These are the default URLs for the various frontend applications.
# They are used by the backend services for redirects (e.g., after OAuth).
# -----------------------------------------------------------------------------
NEXT_PUBLIC_HOME_URL=http://localhost:3001
NEXT_PUBLIC_WEB_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_URL=http://localhost:3003

# -----------------------------------------------------------------------------
# Service Ports
# These define the ports for the standalone backend services.
# -----------------------------------------------------------------------------
AUTH_PORT=3002
BACKEND_PORT=3004

# -----------------------------------------------------------------------------
# API URLs
# These are the base URLs for the backend APIs, used by frontend apps.
# -----------------------------------------------------------------------------
NEXT_PUBLIC_AUTH_API_URL=http://localhost:3002/api
NEXT_PUBLIC_API_BASE_URL=http://localhost:3004/api/v1


# -----------------------------------------------------------------------------
# Optional: Social Login (OAuth) Configuration
# These are only required if you want to enable Google and GitHub login.
# -----------------------------------------------------------------------------
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-google-client-secret

GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret


# -----------------------------------------------------------------------------
# Optional: Google Gemini AI Configuration
# This is only required if you want to use the AI-powered features.
# -----------------------------------------------------------------------------
GEMINI_API_KEY=your_gemini_api_key
