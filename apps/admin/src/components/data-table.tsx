'use client';

// This is a temporary file to resolve the import issue.
// It simply re-exports the real DataTable from the UI package.
export { DataTable } from '@amberops/ui/components/data-table';
