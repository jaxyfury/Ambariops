# MongoDB Connection String
MONGODB_URI=mongodb+srv://amberopsamdin:amberopspassword@amberopsdb.qpiaazs.mongodb.net/?retryWrites=true&w=majority&appName=Amberopsdb

# NextAuth.js Secret
# You can generate a new secret with `openssl rand -base64 32`
NEXTAUTH_SECRET=amberops-secrete-console
NEXTAUTH_URL=http://localhost:3000

# Google OAuth Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# GitHub OAuth Credentials
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google Gemini API Key
GEMINI_API_KEY=

# Application URLs
# These are used by NextAuth for redirects and by the frontend for API calls.
# Ensure they match the ports defined in run.sh and package.json scripts.
NEXT_PUBLIC_WEB_URL=http://localhost:3000
NEXT_PUBLIC_HOME_URL=http://localhost:3001

# The base URL for the API backend.
# For the current setup, this points to the internal Next.js API in the 'web' app.
# In the future, this can be changed to point to a different, dedicated backend server.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api/v1
